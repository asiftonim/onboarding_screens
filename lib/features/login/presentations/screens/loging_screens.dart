import 'package:flutter/material.dart';
class LogingScreens extends StatelessWidget {
  const LogingScreens({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
